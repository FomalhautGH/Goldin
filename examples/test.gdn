rt main() {
    i32 i = 0;

    while (i < 10) {
        i32 j = 0;
        printf("%d\n", i);

        while (j < 1) {
            printf("%d\n", 69);
            j = j + 1;
        }

        i = i + 1;
    }
}
