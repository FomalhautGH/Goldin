// Fibonacci Example

rt main() {
    i32 a = 0;
    i32 b = 1;
    i32 i = 0;

    while (i < 15) {
        printf("%d\n", a);

        i32 c = a + b;
        a = b;
        b = c;
        i = i + 1;
    }
}
